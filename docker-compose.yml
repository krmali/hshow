services:
  hshow:
    build: .
    image: hshow:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      HSHOW_JOURNAL: /data/journal.hledger
    volumes:
      - ${HSHOW_JOURNAL_HOST_PATH:-./testdata/sample.journal}:/data/journal.hledger:ro
